import { DyrektywaDirective } from './dyrektywa.directive';

describe('DyrektywaDirective', () => {
  it('should create an instance', () => {
    const directive = new DyrektywaDirective();
    expect(directive).toBeTruthy();
  });
});
